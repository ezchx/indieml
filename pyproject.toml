[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "indieml"
version = "0.1.4"
description = "A Python and MCP integration library for the IndieML Substance API."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]",
    "httpx"
]

[project.scripts]
indieml = "indieml.mcp.server:main"
